# 个人网站内容更新 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 WorkShowcase 替换为 3 个 AI 辅助工具，将 AboutBeliefs 改造为动画项目紧凑横向展示。

**Architecture:** 两个组件独立修改，WorkShowcase 用卡片网格展示工具，AboutBeliefs 改为横向小方块展示动画项目。MusicSection 和整体布局不变。

**Tech Stack:** Next.js (App Router), React, TypeScript, Tailwind CSS v4, Framer Motion

---

## 文件清单

| 文件 | 操作 |
|------|------|
| `components/WorkShowcase.tsx` | 修改 — 替换项目内容 |
| `components/AboutBeliefs.tsx` | 修改 — 替换为横向动画展示 |
| `docs/superpowers/specs/2026-04-25-个人网站内容更新设计.md` | 参考 — 设计规格文档 |

---

## Task 1: 修改 WorkShowcase — 替换为 AI 工具

**Files:**
- Modify: `components/WorkShowcase.tsx`

**数据替换：**

原来的 `projects` 数组（共 3 项：三国星空、剑来、诛仙）替换为以下内容：

```typescript
const projects = [
  {
    id: 1,
    title: "obs2feishu",
    subtitle: "Obsidian 插件",
    description:
      "Obsidian 是我私人的第二大脑，但当我想把这些笔记分享到飞书时，发现图片链接失效、Callout 样式丢失——格式迁移的摩擦让我干脆放弃了分享。\n\n于是我把这个流程做成了 Obsidian 内置插件：一键导出原生 HTML，图片 Base64 内嵌、Callout 原样保留，复制进飞书编辑器就是干净的一份文档。",
    tags: ["Obsidian", "HTML 导出", "知识管理", "飞书"],
    accent: "#3D7A5F",
    bgColor: "#E8F5EE",
    image: null, // 无图片，用色块代替
    link: "https://github.com/Xxxretaw/obs2feishu",
  },
  {
    id: 2,
    title: "TodoList",
    subtitle: "桌面工具",
    description:
      "太多事情稍纵即逝，等我打开备忘录再打字，它早就被新的念头覆盖了。\n\nWin+H 唤出、打字、回车——手不需要离开键盘，也不需要\"关闭\"这个动作，点击窗口外任意位置自动最小化。数据完全本地，没有任何网络请求。",
    tags: ["PyQt6", "全局热键", "艾森豪威尔矩阵", "本地存储"],
    accent: "#C8763A",
    bgColor: "#FDF3E8",
    image: null,
    link: "https://github.com/Xxxretaw/TodoList",
  },
  {
    id: 3,
    title: "HSV 色彩识别",
    subtitle: "图像标注辅助",
    description:
      "人工判断色调容易受主观干扰，尤其是在大量图像标注工作中，标准不一会导致质量不稳定。\n\n通过 HSV 直方图分析，将图像的色彩分布量化输出，保证判断的一致性和可重复性。",
    tags: ["HSV", "图像处理", "色彩分析", "Python"],
    accent: "#5B6FA8",
    bgColor: "#EBF1FB",
    image: null,
    link: "https://github.com/Xxxretaw",
  },
];
```

**ProjectCard 组件修改：**

1. 当 `project.image` 为 `null` 时，用纯色色块代替图片区域（保持 aspect-ratio 比例，背景用 `project.bgColor`，上方显示一个简单的 SVG 图标如 code brackets 或 terminal 图标）

2. description 字段支持换行（用 `\n` 分段渲染为 `<br />` 或多行 `<p>`）

3. tags 渲染逻辑不变

4. 整体卡片样式保持原有结构（圆角、阴影、hover 上浮）

---

- [x] **Step 1: 读取现有 WorkShowcase.tsx 并确认 project card 渲染逻辑**

读取 `components/WorkShowcase.tsx`，重点关注 `ProjectCard` 组件中图片渲染部分（大约 line 70-92）和 description 渲染部分（大约 line 105-107）。

- [x] **Step 2: 替换 projects 数组内容**

用上述新数组完整替换原有 `projects` 数组。

- [x] **Step 3: 修改 ProjectCard — 无图片时显示色块**

找到图片渲染逻辑（`<img src={assetUrl(project.image)} ...`），改为：
```
{project.image ? (
  <img src={assetUrl(project.image)} ... />
) : (
  <div className="w-full h-full flex items-center justify-center" style={{ backgroundColor: project.bgColor }}>
    {/* SVG code/file icon */}
  </div>
)}
```

- [x] **Step 4: 修改 description 渲染支持换行**

将 `<p className="text-sm leading-relaxed mb-4" style={{ color: "#7A7060" }}>{project.description}</p>`

改为将 description 按 `\n` 分割渲染为多行段落。

- [x] **Step 5: 更新标签文字**

"Selected Work" 下方的 `h2` 保持"我做过的事"不变，或根据需要调整为"我的工具"——以用户确认为准（当前 plan 假设保持"我做过的事"）。

---

## Task 2: 修改 AboutBeliefs — 改为动画项目横向展示

**Files:**
- Modify: `components/AboutBeliefs.tsx`

**变更范围：**
- 标题"我深信的几件事" → "过往作品"
- 移除 `BELIEFS` 数组及相关渲染
- 新增动画项目数据数组
- 新增横向展示组件

**新增数据：**

```typescript
const legacyProjects = [
  {
    title: "三国的星空",
    subtitle: "动画电影 · 后期特效",
    link: "https://v.qq.com/x/cover/mzc00200pawrfhd/f4101qfx0vp.html",
    accent: "#3D7A5F",
  },
  {
    title: "剑来 第二季",
    subtitle: "动画剧集 · 制作统筹",
    link: "https://v.qq.com/x/cover/mzc00200xxpsogl/y4101oeqq9j.html",
    accent: "#C8763A",
  },
  {
    title: "诛仙",
    subtitle: "动画剧集 · 制作统筹",
    link: "https://v.qq.com/x/cover/mzc002003o7sldf/r4101sc41bd.html",
    accent: "#5B6FA8",
  },
];
```

**组件结构（保持 export default function AboutBeliefs 不变）：**

1. 标题区：`<h2>过往作品</h2>`（使用 `--caveat` 字体），移除 "Values" label

2. 横向展示条：一个 `<div className="flex gap-4">` 包含 3 个 `<LegacyProjectItem>`

3. `LegacyProjectItem` 组件：
   - 宽约 200px，高约 80px
   - 左侧 4px 彩色竖条（`accent` 色）
   - 右侧显示标题（`--dm-serif` 字体）+ 副标题（`--inter`，小字）
   - 悬停轻微上浮（`whileHover={{ y: -3 }}`）
   - 点击跳转 link

4. 整体高度约为原来 belief cards 的三分之一（约 100px 左右）

5. 移除所有 doodle 图标装饰（AboutDoodles 可删除或保留但缩小）

6. 保留 `useInView` 动画（入场动画）

---

- [x] **Step 1: 读取 AboutBeliefs.tsx 确认结构**

重点看：标题区（line 97-115）、BELIEFS 数据（line 33-56）、BeliefCard 组件（line 58-87）

- [x] **Step 2: 添加 legacyProjects 数据数组**

在文件顶部 `BELIEFS` 数组位置下方添加 `legacyProjects` 数组。

- [x] **Step 3: 重写 AboutBeliefs 组件的渲染部分**

保留 `export default function AboutBeliefs` 函数签名和 `useInView` 逻辑，只替换 JSX return 的内容。

- [x] **Step 4: 实现 LegacyProjectItem 子组件**

参考 `BeliefCard` 的动画模式（`useInView`、`whileHover`），创建简洁的横向项目展示。

- [x] **Step 5: 清理无用代码**

删除 `BeliefCard` 组件和 `BELIEFS` 数组（确认 AboutDoodles 是否保留——如果保留则保留，否则删除）。

---

## Task 3: 视觉验证

**Files:**
- Modify: 无

- [x] **Step 1: 启动开发服务器并验证变更**

运行 `npm run dev`，浏览器打开 localhost:3000，滚动到 WorkShowcase 和 AboutBeliefs 区域确认：
- WorkShowcase 3 个工具卡片是否正常显示，悬停动效是否正常
- AboutBeliefs 横向展示是否正常，内容是否对齐
- 整体页面是否有明显布局问题

---

## Task 4: Git 提交

- [x] **Step 1: 提交变更**

```bash
git add components/WorkShowcase.tsx components/AboutBeliefs.tsx
git commit -m "feat: 更新个人网站 — WorkShowcase 替换为 AI 工具，AboutBeliefs 改为动画作品展示"
```

---
